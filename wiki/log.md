# Wiki Log

Append-only record of wiki activity. New entries should use:

`## [YYYY-MM-DD] type | Title`

Accepted types: `ingest`, `query`, `lint`, `maintenance`, `note`.

## [2026-06-12] maintenance | Initialize LLM wiki pattern

- Added the raw source layer, generated wiki layer, index, log, templates, workflows, and helper tooling.
- Wired the pattern into the WAT operating instructions.
